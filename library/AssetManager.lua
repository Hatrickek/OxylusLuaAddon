---@meta AssetManager

Assets = {}

--- Loads the mesh if wasn't loaded or has been unloaded
--- @return Mesh # returns the mesh
--- @param path string # asset path to be loaded
function Assets.get_mesh(path) end

--- Loads the audio source if wasn't loaded or has been unloaded
--- @return AudioSource # returns the mesh
--- @param path string # asset path to be loaded
function Assets.get_audio_source(path) end